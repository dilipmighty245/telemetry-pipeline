{{- if and .Values.nexusGateway.enabled .Values.nexusGateway.ingress.enabled -}}
{{- $fullName := include "telemetry-pipeline.fullname" . -}}
{{- $svcPort := .Values.nexusGateway.service.port -}}
{{- if and .Values.nexusGateway.ingress.className (not (hasKey .Values.nexusGateway.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.nexusGateway.ingress.annotations "kubernetes.io/ingress.class" .Values.nexusGateway.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-nexus-gateway
  labels:
    {{- include "telemetry-pipeline.labels" . | nindent 4 }}
    app.kubernetes.io/component: nexus-gateway
  {{- with .Values.nexusGateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.nexusGateway.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.nexusGateway.ingress.className }}
  {{- end }}
  {{- if .Values.nexusGateway.ingress.tls }}
  tls:
    {{- range .Values.nexusGateway.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.nexusGateway.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-nexus-gateway
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-nexus-gateway
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
